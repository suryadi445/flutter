import * as vscode from "vscode";
export declare enum LogVerbosity {
    DEBUG = 0,
    INFO = 1,
    WARN = 2,
    ERROR = 3
}
export declare class Logger {
    private readonly prefix?;
    private channel?;
    protected static verbosity: LogVerbosity;
    constructor(prefix?: string | undefined);
    static setLogVerbosity(verbosity: LogVerbosity): void;
    log(message: string, verbosity: LogVerbosity): void;
    appendLine(message: string, verbosity: LogVerbosity): void;
    append(message: string, verbosity: LogVerbosity): void;
    format(message: string, verbosity: LogVerbosity): string;
    info(...messages: string[]): void;
    warn(...messages: string[]): void;
    error(...messages: string[]): void;
    debug(...messages: string[]): void;
    getChannel(): vscode.OutputChannel | undefined;
    setLoggingChannel(channel: vscode.OutputChannel): void;
    reset(): void;
}
