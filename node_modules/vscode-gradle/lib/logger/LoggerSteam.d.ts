import { Logger, LogVerbosity } from "./Logger";
export declare class LoggerStream {
    private readonly logger;
    private readonly verbosity;
    private buffer;
    constructor(logger: Logger, verbosity: LogVerbosity);
    write(bytes: Uint8Array): void;
    private append;
    getBuffer(): string;
}
