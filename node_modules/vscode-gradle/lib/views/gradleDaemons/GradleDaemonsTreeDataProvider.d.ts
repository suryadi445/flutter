import * as vscode from "vscode";
import { GradleClient } from "../../client";
import { RootProjectsStore } from "../../stores";
export declare class GradleDaemonsTreeDataProvider implements vscode.TreeDataProvider<vscode.TreeItem> {
    private readonly context;
    private readonly rootProjectsStore;
    private readonly client;
    private cancelDeferred?;
    private treeItems;
    private specificVersion;
    private readonly _onDidChangeTreeData;
    readonly onDidChangeTreeData: vscode.Event<vscode.TreeItem | null>;
    constructor(context: vscode.ExtensionContext, rootProjectsStore: RootProjectsStore, client: GradleClient);
    refresh(): void;
    getTreeItem(element: vscode.TreeItem): vscode.TreeItem;
    getChildren(element?: vscode.TreeItem): Promise<vscode.TreeItem[]>;
    private getProjectRootFolders;
    showStoppedDaemons(): void;
    hideStoppedDaemons(): void;
}
