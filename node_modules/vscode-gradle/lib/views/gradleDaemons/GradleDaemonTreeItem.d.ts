import * as vscode from "vscode";
import { DaemonInfo } from "../../proto/gradle_pb";
export declare class GradleDaemonTreeItem extends vscode.TreeItem {
    private readonly context;
    readonly label: string;
    private readonly daemonInfo;
    private status;
    constructor(context: vscode.ExtensionContext, label: string, daemonInfo: DaemonInfo);
    get pid(): string;
}
