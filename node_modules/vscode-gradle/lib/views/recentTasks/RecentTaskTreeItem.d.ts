import * as vscode from "vscode";
import { GradleTaskTreeItem } from "..";
import { Icons } from "../../icons";
import { TaskTerminalsStore } from "../../stores";
export declare class RecentTaskTreeItem extends GradleTaskTreeItem {
    private readonly taskTerminalsStore;
    protected readonly javaDebug: boolean;
    constructor(parentTreeItem: vscode.TreeItem, task: vscode.Task, label: string, description: string, icons: Icons, taskTerminalsStore: TaskTerminalsStore, javaDebug: boolean);
    setContext(): void;
}
