import * as vscode from "vscode";
import { PinnedTaskTreeItem } from ".";
import { GradleTaskProvider } from "../../tasks";
import { PinnedTasksStore, RootProjectsStore } from "../../stores";
import { Icons } from "../../icons";
import { GradleClient } from "../../client";
export declare function getPinnedTasksTreeItemMap(): Map<string, PinnedTaskTreeItem>;
export declare class PinnedTasksTreeDataProvider implements vscode.TreeDataProvider<vscode.TreeItem> {
    private readonly pinnedTasksStore;
    private readonly rootProjectsStore;
    private readonly gradleTaskProvider;
    private readonly icons;
    private readonly client;
    private readonly _onDidChangeTreeData;
    readonly onDidChangeTreeData: vscode.Event<vscode.TreeItem | null>;
    constructor(pinnedTasksStore: PinnedTasksStore, rootProjectsStore: RootProjectsStore, gradleTaskProvider: GradleTaskProvider, icons: Icons, client: GradleClient);
    getStore(): PinnedTasksStore;
    getTreeItem(element: vscode.TreeItem): vscode.TreeItem;
    refresh(treeItem?: vscode.TreeItem | null): void;
    getParent(element: vscode.TreeItem): vscode.TreeItem | null;
    getChildren(element?: vscode.TreeItem): Promise<vscode.TreeItem[]>;
    private buildTreeItems;
}
