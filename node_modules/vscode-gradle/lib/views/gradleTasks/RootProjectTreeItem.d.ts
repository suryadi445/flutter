import * as vscode from "vscode";
import { ProjectTreeItem } from ".";
export declare class RootProjectTreeItem extends vscode.TreeItem {
    readonly projects: ProjectTreeItem[];
    readonly parentTreeItem: RootProjectTreeItem | null;
    constructor(name: string, resourceUri: vscode.Uri);
    addProject(project: ProjectTreeItem): void;
}
