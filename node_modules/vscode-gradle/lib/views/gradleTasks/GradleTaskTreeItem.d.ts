import * as vscode from "vscode";
import { Icons } from "../../icons";
export declare class GradleTaskTreeItem extends vscode.TreeItem {
    readonly parentTreeItem: vscode.TreeItem;
    readonly task: vscode.Task;
    readonly label: string;
    tooltip: string;
    description: string;
    protected readonly icons: Icons;
    protected readonly javaDebug: boolean;
    constructor(parentTreeItem: vscode.TreeItem, task: vscode.Task, label: string, tooltip: string, description: string, icons: Icons, javaDebug: boolean);
    setContext(): void;
    protected setIconState(): void;
}
