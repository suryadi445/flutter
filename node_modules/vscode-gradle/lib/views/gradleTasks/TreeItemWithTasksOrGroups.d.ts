import * as vscode from "vscode";
import { GradleTaskTreeItem } from ".";
import { GroupTreeItem } from "..";
export declare class TreeItemWithTasksOrGroups extends vscode.TreeItem {
    private readonly _tasks;
    private readonly _groups;
    readonly parentTreeItem?: vscode.TreeItem;
    readonly iconPath: vscode.ThemeIcon;
    readonly contextValue = "folder";
    constructor(name: string, parentTreeItem?: vscode.TreeItem, resourceUri?: vscode.Uri, collapsibleState?: vscode.TreeItemCollapsibleState);
    addTask(task: GradleTaskTreeItem): void;
    get tasks(): GradleTaskTreeItem[];
    addGroup(group: GroupTreeItem): void;
    get groups(): GroupTreeItem[];
}
