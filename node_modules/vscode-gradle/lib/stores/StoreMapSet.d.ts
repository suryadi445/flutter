import { StoreMap } from "./StoreMap";
export declare class StoreMapSet<K, V> extends StoreMap<K, Set<V>> {
    addEntry(key: K, value: V, fireOnDidChange?: boolean): void;
    removeEntry(key: K, value: V, fireOnDidChange?: boolean): void;
}
