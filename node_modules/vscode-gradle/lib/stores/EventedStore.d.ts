import * as vscode from "vscode";
export declare abstract class EventedStore<V> implements vscode.Disposable {
    private readonly _onDidChange;
    readonly onDidChange: vscode.Event<V | null>;
    fireOnDidChange(value: V | null): void;
    dispose(): void;
}
