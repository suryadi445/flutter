import { EventedStore } from ".";
export declare abstract class StoreMap<K, V> extends EventedStore<V> {
    private readonly data;
    getItem(key: K): V | void;
    getData(): Map<K, V>;
    get(key: K): V | undefined;
    clear(fireOnDidChange?: boolean): void;
    setItem(key: K, value: V, fireOnDidChange?: boolean): void;
    removeItem(key: K, fireOnDidChange?: boolean): void;
}
