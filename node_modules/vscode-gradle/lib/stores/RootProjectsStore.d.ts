import { StoreMap } from ".";
import { RootProject } from "../rootProject/RootProject";
export declare class RootProjectsStore extends StoreMap<string, RootProject> {
    private isPopulated;
    private populatePromise;
    populate(): Promise<void>;
    private setRootProjectFolder;
    private static isGradleFileExists;
    getProjectRoots(): Promise<RootProject[]>;
    getProjectRootsWithUniqueVersions(): Promise<RootProject[]>;
    clear(fireOnDidChange?: boolean): void;
}
