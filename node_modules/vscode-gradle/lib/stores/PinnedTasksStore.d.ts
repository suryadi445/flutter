import * as vscode from "vscode";
import { TaskStore } from ".";
export declare class PinnedTasksStore extends TaskStore {
    private readonly context;
    constructor(context: vscode.ExtensionContext);
    fireOnDidChange(): Promise<void>;
}
