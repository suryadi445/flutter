import * as vscode from "vscode";
export declare class FileWatcher implements vscode.Disposable {
    protected readonly fileGlob: string;
    private readonly _onDidChange;
    private readonly _onDidOpen;
    readonly onDidChange: vscode.Event<vscode.Uri>;
    readonly onDidOpen: vscode.Event<vscode.Uri>;
    private onDidSaveDisposable;
    private onDidDeleteDisposable;
    private onDidCreateDisposable;
    private onDidOpenDisposable;
    constructor(fileGlob: string);
    private isFileMatching;
    fireOnDidChange: (uri: vscode.Uri) => void;
    fireOnDidOpen: (uri: vscode.Uri) => void;
    dispose(): void;
}
