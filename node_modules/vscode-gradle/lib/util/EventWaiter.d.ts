import * as vscode from "vscode";
declare type callback = () => void;
export declare class EventWaiter<T = null> {
    private readonly event;
    private eventRun;
    constructor(event: vscode.Event<T>);
    waitForEvent: (callback?: callback | undefined) => void;
    wait: () => Promise<void>;
    reset(): void;
}
export {};
