import * as vscode from "vscode";
import { Logger } from "../logger/index";
export interface ServerOptions {
    host: string;
}
export declare class GradleServer implements vscode.Disposable {
    private readonly opts;
    private readonly context;
    private readonly logger;
    private readonly _onDidStart;
    private readonly _onDidStop;
    private ready;
    private port;
    private restarting;
    readonly onDidStart: vscode.Event<null>;
    readonly onDidStop: vscode.Event<null>;
    private process?;
    constructor(opts: ServerOptions, context: vscode.ExtensionContext, logger: Logger);
    start(): Promise<void>;
    isReady(): boolean;
    showRestartMessage(): Promise<void>;
    restart(): Promise<void>;
    private logOutput;
    private killProcess;
    private handleServerStartError;
    private fireOnStart;
    stop(): void;
    dispose(): void;
    getPort(): number | undefined;
    getOpts(): ServerOptions;
}
