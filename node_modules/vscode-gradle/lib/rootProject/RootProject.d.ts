import * as vscode from "vscode";
import { Environment } from "../proto/gradle_pb";
export declare class RootProject {
    private readonly workspaceFolder;
    private readonly projectUri;
    private environment?;
    constructor(workspaceFolder: vscode.WorkspaceFolder, projectUri: vscode.Uri);
    setEnvironment(environment: Environment): void;
    getEnvironment(): Environment | undefined;
    getWorkspaceFolder(): vscode.WorkspaceFolder;
    getProjectUri(): vscode.Uri;
}
